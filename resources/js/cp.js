/**
 * Statamic 6 Control Panel extensions.
 * https://statamic.dev/extending/control-panel
 *
 * To enable the example fieldtype:
 * 1. Uncomment the block below.
 * 2. In vite.config.js, add 'resources/js/cp.js' to the laravel plugin input array
 *    and add the Statamic Vite plugin (see https://statamic.dev/extending/control-panel#adding-css-and-js-assets).
 */

/** Example Fieldtype (Statamic 6 / Vue 3)
import ExampleFieldtype from './components/fieldtypes/ExampleFieldtype.vue';

Statamic.booting(() => {
    Statamic.$components.register('example-fieldtype', ExampleFieldtype);
});
*/
